# encoding: utf-8

source 'https://rubygems.org'

gem 'jekyll', '~> 4.x'

group :jekyll_plugins do
  gem 'jekyll-paginate'
  gem 'jekyll-archives'
  gem 'jekyll-compose'
  gem 'jekyll-feed'
  gem 'jekyll-seo-tag'
end

group :test do
  gem 'html-proofer', '~> 5.x'
end
